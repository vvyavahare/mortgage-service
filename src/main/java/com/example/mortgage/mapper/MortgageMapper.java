package com.example.mortgage.mapper;

import org.mapstruct.Mapper;

@Mapper(componentModel = "spring")
public interface MortgageMapper {
    // placeholder for future mapping; using records makes mapping trivial
}
